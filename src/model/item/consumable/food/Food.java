package model.item.consumable.food;

import model.item.consumable.Consumable;

public abstract class Food extends Consumable
{
    public Food(int hp, int weight)
    {
        super(hp, weight);
    }
}
